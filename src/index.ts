import * as interactionsImport from './interactions';
import * as register from './register';
export const interactions = interactionsImport;
export const registerInteractions = register.registerInteractions;